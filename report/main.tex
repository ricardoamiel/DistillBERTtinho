\documentclass{article}

\usepackage[preprint]{neurips_2025}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage[skip=3pt,font=small]{caption}
\usepackage{graphicx}

\graphicspath{{../artifacts/figures/}}

% Let floats take more of a page so tables do not get deferred past the text.
\renewcommand{\topfraction}{0.92}
\renewcommand{\bottomfraction}{0.85}
\renewcommand{\textfraction}{0.06}
\renewcommand{\floatpagefraction}{0.80}
\setlength{\textfloatsep}{10pt plus 2pt minus 2pt}
\setlength{\intextsep}{10pt plus 2pt minus 2pt}

% Measured quantities are generated by src/figures.py so no number is typed by hand.
% Fallbacks so the report always compiles, even before every run has finished.
% src/figures.py overwrites each of these with the measured value.
\providecommand{\accRetention}{\textbf{[pending]}}
\providecommand{\bertWins}{\textbf{[pending]}}
\providecommand{\bestVariant}{\textbf{[pending]}}
\providecommand{\bestVariantFone}{\textbf{[pending]}}
\providecommand{\ckaEmbeddings}{\textbf{[pending]}}
\providecommand{\ckaMid}{\textbf{[pending]}}
\providecommand{\ckaMidBertLayer}{\textbf{[pending]}}
\providecommand{\ckaMidDistilLayer}{\textbf{[pending]}}
\providecommand{\ckaTop}{\textbf{[pending]}}
\providecommand{\cpuLatencyBert}{\textbf{[pending]}}
\providecommand{\cpuLatencyDistil}{\textbf{[pending]}}
\providecommand{\distilTiesOrWins}{\textbf{[pending]}}
\providecommand{\epochBestMax}{\textbf{[pending]}}
\providecommand{\epochBestMin}{\textbf{[pending]}}
\providecommand{\epochGainOverTwo}{\textbf{[pending]}}
\providecommand{\epochStudyRuns}{\textbf{[pending]}}
\providecommand{\frozenGap}{\textbf{[pending]}}
\providecommand{\gpuLatencyBert}{\textbf{[pending]}}
\providecommand{\gpuLatencyDistil}{\textbf{[pending]}}
\providecommand{\gpuSpeedup}{\textbf{[pending]}}
\providecommand{\halfFrozenGap}{\textbf{[pending]}}
\providecommand{\headSpread}{\textbf{[pending]}}
\providecommand{\maxAccGap}{\textbf{[pending]}}
\providecommand{\maxGapDataset}{\textbf{[pending]}}
\providecommand{\meanAccGap}{\textbf{[pending]}}
\providecommand{\memReduction}{\textbf{[pending]}}
\providecommand{\minAccGap}{\textbf{[pending]}}
\providecommand{\minGapDataset}{\textbf{[pending]}}
\providecommand{\nDatasets}{\textbf{[pending]}}
\providecommand{\nRuns}{\textbf{[pending]}}
\providecommand{\nWords}{\textbf{[pending]}}
\providecommand{\paramReduction}{\textbf{[pending]}}
\providecommand{\purityBert}{\textbf{[pending]}}
\providecommand{\purityDistil}{\textbf{[pending]}}
\providecommand{\throughputBert}{\textbf{[pending]}}
\providecommand{\throughputDistil}{\textbf{[pending]}}
\providecommand{\throughputSpeedup}{\textbf{[pending]}}
\providecommand{\trainMemBert}{\textbf{[pending]}}
\providecommand{\trainMemDistil}{\textbf{[pending]}}
\providecommand{\trainMinutesBert}{\textbf{[pending]}}
\providecommand{\trainMinutesDistil}{\textbf{[pending]}}
\providecommand{\wordCka}{\textbf{[pending]}}
\providecommand{\wordOverlap}{\textbf{[pending]}}
\providecommand{\wordOverlapPct}{\textbf{[pending]}}
\input{../artifacts/tables/numbers.tex}

\title{DistilBERT and BERT for Text Classification:\\ Performance, Efficiency and the Role of the Classifier Head}

\author{%
  Ricardo Amiel Acu\~na Villogas \quad Juan Leibniz Aquino Espinoza \quad Josu\'e Nehem\'ias Velo Poma \\
  Universidad de Ingenier\'ia y Tecnolog\'ia (UTEC) \\
  \texttt{\{ricardo.acuna, juan.aquino, josue.velo\}@utec.edu.pe}
}

\begin{document}

\maketitle

\begin{abstract}
Knowledge distillation promises a smaller encoder that behaves like its teacher, but the size of the
concession is rarely measured under one controlled protocol. We fine tune DistilBERT and BERT base under an
identical classifier, optimiser and data budget on \nDatasets{} text classification datasets: AG News,
SST 2, Yelp Polarity and Yelp Review Full. DistilBERT holds \paramReduction{} percent fewer parameters and
reaches \accRetention{} percent of the accuracy of BERT, a mean gap of \meanAccGap{} points ranging from
\minAccGap{} to \maxAccGap{} across datasets, in exchange for \throughputSpeedup{} times the inference
throughput and \memReduction{} percent less peak GPU memory in training. A seven way ablation over the
classifier shows its shape is almost irrelevant: width and depth move F1 macro by at most \headSpread{}
points, while freezing the transformer costs \frozenGap{}. Over \nWords{} words, \wordOverlapPct{} percent
of nearest neighbours are shared between the two pretrained encoders, at a linear CKA of \wordCka{}. What
distillation gave up is capacity, not the organisation of the space.
\end{abstract}

\section{Introduction}

BERT \citep{devlin2019bert} made pretrained bidirectional transformers \citep{vaswani2017attention} the
default starting point for text classification, at a cost that is awkward in deployment: 110 million
parameters and a twelve block forward pass per input. DistilBERT \citep{sanh2019distilbert} answers with
knowledge distillation \citep{hinton2015distilling}, training a six block student against the teacher
output distribution, its hidden states and the masked language modelling objective, and reports roughly
97 percent of the GLUE score \citep{wang2018glue} of BERT with 40 percent fewer parameters.

That headline is an aggregate over nine tasks of different sizes and metrics, so it does not say what a
practitioner needs before choosing a backbone for one problem: how large the gap is here, what it buys in
latency and memory, and whether any of it can be recovered by spending capacity on the classifier instead
of the encoder. We answer those questions under one protocol: a like for like comparison on four datasets
reporting performance next to parameters, latency and peak GPU memory on the same device; an ablation over
the classifier along the three axes a practitioner controls, freezing, width and depth, with the winning
variant rebuilt on BERT; and a representation level analysis, released as an interactive tool.

\section{Approach}

\paragraph{What distillation changed.}
DistilBERT keeps the hidden size of 768 and the vocabulary of BERT base, removes every second transformer
block leaving six, and drops the token type embeddings and the pooler. The student is initialised from
alternate teacher layers and trained with three losses: distillation against the softened teacher
distribution, masked language modelling, and a cosine term aligning student and teacher hidden state
directions \citep{sanh2019distilbert}. Depth is the axis that was cut; width, vocabulary and attention are
untouched.

\paragraph{One classifier for both backbones.}
Both backbones are wrapped in the same module. The final hidden state of the \texttt{[CLS]} position,
$h \in \mathbb{R}^{768}$, is passed through an MLP $\hat{y} = W_L\,\sigma(\cdots \sigma(W_1\,
\mathrm{drop}(h)) \cdots)$ with $\sigma = \mathrm{ReLU}$ and dropout before every linear map. We
deliberately do not use the BERT pooler, which DistilBERT does not have, so both models are read the same
way and no architectural advantage enters through pooling. The baseline head is one hidden layer of 768
units, reproducing the default DistilBERT design, and the same head exposes the three ablation axes:
freezing covers the embeddings and the first $n$ blocks, with $n$ equal to all blocks giving a linear
probe; width is the units per hidden layer; depth is the number of hidden layers.

\paragraph{What we measure.}
Accuracy with macro averaged precision, recall and F1 on the test split, macro averaged so the five class
Yelp task is not dominated by its easiest classes. Efficiency is parameter count, single example latency,
throughput at batch 32 and peak GPU memory. Latency is the median of 200 timed float32 passes after 20
warmup passes with explicit device synchronisation; memory is read from the CUDA allocator, not the driver.
Training and validation loss are recorded at twelve equally spaced iterations, the validation loss on a
held out slice of train, so the test split is never touched during optimisation.

\section{Experiments}

\paragraph{Setup.}
AG News is four way topic classification \citep{zhang2015character}; SST 2 is binary sentiment
\citep{socher2013recursive} through GLUE \citep{wang2018glue}, whose labelled validation split serves as the
test set as usual; Yelp Polarity and Yelp Review Full are binary and five star review sentiment
\citep{zhang2015character}, each subsampled to 100{,}000 training and 10{,}000 test examples. Five percent
of every training set is held out for the loss curves. Every run uses AdamW \citep{loshchilov2019adamw} at
$2 \times 10^{-5}$ on the backbone and $10^{-4}$ on the head, weight decay 0.01, linear decay with 6 percent
warmup, batch 32, clipping at 1.0 and bfloat16 autocast, with maximum length 64 for SST 2, 128 for AG News
and 256 for Yelp. Two epochs sits inside the fine tuning range recommended by \citet{devlin2019bert}, and
the loss curves below confirm both models have converged by then. All \nRuns{} runs in this study used one
NVIDIA RTX A6000 and the same seed, so latency and memory are comparable across rows.

\begin{figure}[!tb]
  \centering
  \includegraphics[width=\linewidth]{fig1_params_vs_accuracy_bubble.pdf}
  \caption{Parameters against accuracy. Each line joins the two backbones on one dataset; bubble area is
  single example GPU latency. The lines are close to flat, which is this study in one picture.}
  \label{fig:bubble}
\end{figure}

\paragraph{Main comparison.}
Table \ref{tab:main} gives the four performance metrics and Figure \ref{fig:bubble} the required parameter
against accuracy view. The gap averages \meanAccGap{} accuracy points in favour of BERT but is far from
uniform, spanning \minAccGap{} points on \minGapDataset{} to \maxAccGap{} on \maxGapDataset{}: BERT is
clearly ahead on \bertWins{} of the \nDatasets{} datasets and level on the other \distilTiesOrWins{}. Where
a task is close to saturated the two models are indistinguishable, and depth only pays on the harder,
finer grained label set.

\input{../artifacts/tables/table_main_results.tex}

\begin{figure}[!tb]
  \centering
  \includegraphics[width=\linewidth]{fig3_loss_curves.pdf}
  \caption{Iterations against training loss (solid) and validation loss (dashed). Both models flatten at
  the same point in the schedule; BERT settles lower.}
  \label{fig:loss}
\end{figure}

\paragraph{How many epochs are needed.}
Two epochs is a choice, so we test it. Table \ref{tab:epochs} trains both backbones on SST 2 and AG News
to four epochs, the top of the published range, scoring the full validation split at every epoch boundary
and selecting by validation F1 macro. The best epoch is \epochBestMin{} or \epochBestMax{} in every one of
the \epochStudyRuns{} runs, and the most that any run gains over epoch two is \epochGainOverTwo{} F1 macro
points. Two epochs is therefore not a budget compromise, it is where these datasets saturate.

\input{../artifacts/tables/table_epochs.tex}

\paragraph{Training dynamics.}
Figure \ref{fig:loss} matters beyond bookkeeping. If DistilBERT were merely underfitting, its training loss
would sit high and still be falling at the end of the schedule. It does not: both models flatten together,
so the distance between the two validation curves is a capacity gap, not an optimisation gap.

\paragraph{Efficiency.}
Table \ref{tab:eff} gives the cost side, and the shape of the saving is worth reading carefully. Halving
the depth should buy a factor of two, and it does exactly that where the device is actually busy:
DistilBERT sustains \throughputDistil{} samples per second at batch 32 against \throughputBert{}, a factor
of \throughputSpeedup{}, and on CPU it needs \cpuLatencyDistil{} ms per example against \cpuLatencyBert{}.
At batch one on the GPU the same models are only \gpuSpeedup{} times apart, \gpuLatencyDistil{} ms against
\gpuLatencyBert{} ms, because a single short sequence leaves the device idle between kernel launches and
the fixed launch cost, not the twelve or six blocks, sets the clock. Peak GPU memory in training falls from
\trainMemBert{} to \trainMemDistil{} GiB, \memReduction{} percent less, and the four runs take
\trainMinutesDistil{} minutes instead of \trainMinutesBert{}.

\input{../artifacts/tables/table_efficiency.tex}

\section{Analysis}

\begin{figure}[!tb]
  \centering
  \includegraphics[width=\linewidth]{fig5_ablation.pdf}
  \caption{Classifier ablation on DistilBERT. Left and centre: F1 macro per variant. Right: trainable
  parameters against mean F1 macro.}
  \label{fig:ablation}
\end{figure}

\paragraph{The classifier head barely matters.}
Across the four head shape variants and the baseline, the full spread in mean F1 macro is \headSpread{}
points (Figure \ref{fig:ablation}, Table \ref{tab:ablation}): narrowing the hidden layer to 128 units,
widening it to 2048, stacking three hidden layers and removing it entirely all land inside that band. After
fine tuning the \texttt{[CLS]} vector is close to linearly separable here, so capacity added on top of it
has nothing left to extract.

\input{../artifacts/tables/table_ablation.tex}

\paragraph{Freezing is the axis that matters.}
Freezing the whole transformer costs \frozenGap{} F1 macro points against the baseline, an order of
magnitude more than any head shape change. Freezing only the lower three blocks costs \halfFrozenGap{}
points, which locates the task specific adaptation in the upper half of the encoder. The practical reading
is to spend compute on making the transformer trainable rather than on the classifier. The selected variant
is \bestVariant{} at \bestVariantFone{} mean F1 macro.

\paragraph{The winning variant on BERT.}
Table \ref{tab:best} rebuilds that variant on BERT base. The ordering is unchanged and the gap stays in the
band of the main comparison, so the ablation conclusion belongs to the fine tuning setup rather than to
DistilBERT specifically. The stored confusion matrices agree: both models concentrate their residual errors
in the same place, most visibly between the Business and Sci/Tech classes of AG News.

\input{../artifacts/tables/table_best_on_bert.tex}

\paragraph{Do the two encoders see language the same way?}
The accuracy gap says how often the models agree on a label, not whether they agree on the geometry
underneath. We embed \nWords{} words spanning 20 topics with each pretrained encoder and compare the two
768 dimensional spaces with linear centred kernel alignment \citep{kornblith2019similarity}, matching
student block $d$ to teacher block $2d$, the layer it was initialised from. Alignment is near perfect at
the embeddings (\ckaEmbeddings{}), still high at mid depth (\ckaMid{} for student block
\ckaMidDistilLayer{} against teacher block \ckaMidBertLayer{}) and falls to \ckaTop{} at the top, so
distillation preserved the lower and middle geometry and let the task specialised upper layers drift.
Reading words from the layer averaged, mean centred space, \wordOverlap{} of the ten nearest neighbours
BERT assigns to a word are also chosen by DistilBERT, and \purityDistil{} percent of DistilBERT neighbours
share the topic of their query word against \purityBert{} percent for BERT, so the smaller model is
marginally tidier here. An interactive version ships with the code.

\section{Conclusion}

DistilBERT keeps \accRetention{} percent of the accuracy of BERT base on four datasets while holding
\paramReduction{} percent fewer parameters, reaching \throughputSpeedup{} times the inference throughput
and training in \memReduction{} percent less GPU memory, and two epochs is enough for both. The classifier is not a
useful place to spend that budget: head width and depth move F1 macro by at most \headSpread{} points
across seven variants, while freezing the transformer costs \frozenGap{}. Alignment between the two
encoders is near perfect at the embeddings and decays with depth, so what distillation gave up is capacity
in the upper layers, not the organisation of the space. Under a latency or memory budget DistilBERT is the
right default here, and the budget is better spent unfreezing the encoder than enlarging the classifier.
One caveat: Yelp was subsampled and each run used one seed, so differences under half a point are noise.

\bibliographystyle{plainnat}
\bibliography{refs}

\end{document}
